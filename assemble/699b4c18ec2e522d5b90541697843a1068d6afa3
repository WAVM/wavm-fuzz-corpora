(module
  
 ( custom_section "a cul is thpaylnad")
  
  
 (custom_section "a`=ustom section")(custom_section "a cl  sshe payloa")
  
   (custom_section "a custom si64.or"
    "thd")

  
  
  (custom_section "a cu(ta culs isload")
  

(custom_section "a`c~ishe payload")  (custom_section "L cust~ sectithd")
  
  
  (custom_section "au} sectiohe om\155")
  
  )