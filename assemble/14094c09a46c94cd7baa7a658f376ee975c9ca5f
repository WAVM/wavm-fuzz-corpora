(func table.size $c table.size $nc table.size $gnc table.size $unc table.size $nc table.size $nc table.size $c table.size $nc table.size $..snc table.size $nc table.size $.. table.size $nc table.size $nc table.size $..snc table.size $nc table.size $..s  )